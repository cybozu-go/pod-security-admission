apiVersion: v1
kind: Pod
metadata:
  name: noncore-volume
spec:
  securityContext:
    runAsNonRoot: true
  containers:
    - name: ubuntu
      image: quay.io/cybozu/ubuntu
  volumes:
    - name: nfs
      nfs:
        path: /mnt/nfs
        server: localhost
